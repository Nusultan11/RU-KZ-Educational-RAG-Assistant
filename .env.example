# Example environment file. Do not store real secrets here.

APP_ENV=local
LOG_LEVEL=INFO

# Put real values in a local .env file or secret manager, not in Git.
OPENAI_API_KEY=
HF_TOKEN=
DATABASE_URL=

